#https://github.com/azz/pretty-quick
# yarn pretty-quick --staged

# https://github.com/okonet/lint-staged#configuration.
# yarn lint-staged


# 获取 Git 仓库根目录
REPO_ROOT=$(git rev-parse --show-toplevel)

# 切换到仓库根目录，并执行脚本
node "$REPO_ROOT/scripts/monorepo-husky-lint-staged.cjs"

